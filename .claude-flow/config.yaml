version: "3.0.0"
swarm:
  topology: hierarchical-mesh
  maxAgents: 15
  autoScale: true
memory:
  backend: hybrid
  enableHNSW: true
  persistPath: .claude-flow/data
neural:
  enabled: true
hooks:
  enabled: true
mcp:
  autoStart: true
